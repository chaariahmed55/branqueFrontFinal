import { Elementpublication } from './elementpublication';

describe('Elementpublication', () => {
  it('should create an instance', () => {
    expect(new Elementpublication()).toBeTruthy();
  });
});
